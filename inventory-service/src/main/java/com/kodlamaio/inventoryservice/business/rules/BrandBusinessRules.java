package com.kodlamaio.inventoryservice.business.rules;

import com.kodlamaio.inventoryservice.repository.BrandRepository;
import lombok.AllArgsConstructor;
import org.springframework.stereotype.Service;


@Service
@AllArgsConstructor
public class BrandBusinessRules {
//    private final BrandRepository repository;
//
//    public void checkIfBrandExists(int id) {
//        if (!repository.existsById(id)) throw new BusinessException(Messages.Car.NotExists);
//    }
//
//    public void checkIfBrandExistsByName(String name) {
//        if (!repository.existsByNameIgnoreCase(name)) throw new BusinessException(Messages.Car.Exists);
//    }
}

